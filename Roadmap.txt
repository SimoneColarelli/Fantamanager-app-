Next features:
- Main calculation features:
	- Complete update of players
	- Only quotazione update
	- Only in serie a update

- Test calculation features DONE

- Repair the refresh function, it does not work DONE

- Mercato: DONE
	- definition of a new model: operazione di mercato
	- connection between an operazione di mercato and giocatori and fantasquadre tables

- Undo feature for all db commit DONE
- Add sum of rosa value and patrimonio columns read-only (not in db) DONE
- free editable fields option DONE
- Add for each cessione definitiva the possibility of extending the contract according to the aumento contratto rule: January/February acquisitions pay 30% and extend by 1 year; August/September acquisitions pay 35% and extend by 2 years; all other acquisition months are blocked.
- fix scambio prestiti operation card ui
- Introduce Supabase/Postgres as the main persistent database for shared and durable data, while keeping SQLite as a local fast store for undo snapshots, quick recovery, local cache, and offline/retry support.
- Supabase first migration PREPARED:
	- initial Postgres schema in `supabase/migrations/20260714000100_initial_schema.sql`
	- SQLite-to-Supabase seed exporter in `scripts/export_sqlite_to_supabase_seed.py`
	- generated seed in `supabase/seed.sql`
	- operational notes in `docs/SUPABASE_MIGRATION.md`
- Hybrid Supabase + SQLite persistence IMPLEMENTED:
	- optional runtime sync configured by local `.env`
	- local SQLite remains the undo/recovery store
	- manual CLI/menu push and pull snapshot sync
	- auto post-commit push mode available with `FANTAMANAGER_SYNC_MODE=auto`
	- local `sync_state` migration records latest sync status
- Reliability architecture DECIDED:
	- single desktop client, no multi-client merge workflow
	- JSON exports are user-facing backups, UI label changed from Export to Backup
	- snapshot backups and semantic undo are separate features
	- semantic undo must reverse one audited transaction/operation, not restore the whole dataset
	- sync reliability will use local `sync_outbox`, retry state, and `entity_versions`
	- `semantic_undo_log` will hold before/after snapshots for auditable rollback
- Semantic undo audit STARTED:
	- all market operations write before/after rows to `semantic_undo_log`
	- every market operation type has an explicit inverse action definition
	- auction-created players are logged as created entities
	- svincolo-deleted players are logged as deleted entities
- Semantic undo execution IMPLEMENTED:
	- inverse actions can be executed from `semantic_undo_log` by transaction id
	- strict conflict detection blocks undo when touched entities changed later
	- CLI: `scripts/semantic_undo.py list|undo`
	- UI: `Modifica > Annulla operazione auditata...`
	- remote recovery sync IMPLEMENTED:
		- Supabase migration for `semantic_undo_log`
		- Supabase migration for `entity_versions`
		- snapshot push/pull includes audit/version tables
		- `sync_outbox` remains local-only retry state
	- next step: add a visible rettifica/undo card in storico operazioni
- P1 architecture refactor STARTED:
	- service layer `services/mercato_service.py`
	- command/DTO objects
	- UnitOfWork boundary
	- lightweight schema migrations with `schema_migrations`
	- normalized giocatori -> fantasquadre references IMPLEMENTED:
		- added nullable `giocatori.fantasquadra_id`
		- added nullable `giocatori.prestito_a_fantasquadra_id`
		- kept legacy text fields `squadra` and `in_prestito_a` for UI compatibility
		- added SQLite/Supabase migrations and seed export compatibility

In client app:
- fix simulatore behaviour: when calculating a cessione without contropartite (Team B pays N fm for players of Team A), throw a warning if the amount of FM is less then the sum of valore svincolo of players

Perfect. Now i would like to implement a new operation: "Aumento contratto". It works as following: it has to display only a fantasquadra selector and the button to select players. When selected, the function triggered when pressing on "registra" is: for players bought in January or February, the fantasquadra pays 30% of valore svincolo and the year of "scadenza_contratto" increases by 1; for players bought in August or September, the fantasquadra pays 35% of valore svincolo and the year increases by 2; all other purchase months are blocked. Of course the function has to be allowed only if fm amount of the fantasquadra is enough to do that. 
In storico operazioni section, this type of operation has to be displayed with a card like "svincolo" or "asta", that is without double arrows indicating an exchange, and player rows displaying the name of player as main info, the amount paid for the increased contratto and the new date of scadenza contratto as secondary info. Under player rows section, display the sum of paid amounts.
Once defined this operation, we need to add in all operation "acquisto definitivo", "scambio definitivo" and "asta" the possibility to flag the option to extend contratto according to the rules previously defined. Of course, the amount to be paid for contratto increase has to be calculated from the valore svincolo calculated after the operation
